# Get a free key at https://www.themoviedb.org/settings/api
# Optional — Fountain Home falls back to a shared demo key if this is unset,
# but shared keys get reused across a lot of tutorial projects and can be
# rate-limited at busy times, so your own key is more reliable.
VITE_TMDB_API_KEY=
